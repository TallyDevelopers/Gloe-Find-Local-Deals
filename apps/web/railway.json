{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd ../.. && npm install --workspaces --include-workspace-root && npm run --workspace=@gloe/web build"
  },
  "deploy": {
    "startCommand": "cd ../.. && npm run --workspace=@gloe/web start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
